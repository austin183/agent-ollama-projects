# Phase 3 — N-32: KB-12 — rapid live-region coalescing (docs only)

**Source:** review N-32 — rapid live-region succession at high BPM with count-in 1 ("Count-in started" → "Song started" <1 s apart) may be coalesced or dropped by some assistive technology.

## Change (docs only)

Record **KB-12** in the v1 `context.md`: accepted AT behavior — the click *sounds* are the primary signal; the polite live region is secondary. No code change (no debouncing of announcements).

## Success criteria

- [ ] KB-12 present in the v1 `context.md`. No code changes.

Status: ✅ done (2026-08-22)
