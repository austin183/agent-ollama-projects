# Metronomad — Project Timeline

Milestone history, coarse-grained. Phase-level detail lives in the plan directories; session-level detail in `sessions/*.json` (this folder).

## v1 — sample-accurate count-in app (Aug 17–21, 2026)

- **Aug 17** — v1 specification + BDD implementation plan (8 phases). Phase 1 scaffold: single `index.html`, Vue 3 + Howler from CDN, ES-module factory wiring, 7 smoke E2E.
- **Aug 18** — Phase 2 pure functions (`beatGrid`, `timeFormat`, `paramClamps`; T-01…T-34). Phase 3 file loading (decode + codec gate + 30-min guard + object-URL lifecycle; F-01…F-09, H-01…H-02).
- **Aug 19** — Phase 4 `PlaybackEngine`: D4 generation counter, D5 single sample-accurate `source.start(when, offset)`, ~25 ms lookahead click scheduler (P-01…P-14, H-03). Phase 5 UI integration (Play/Stop/Restart/Preview, state machine, non-blocking error overlays).
- **Aug 20** — Phase 6 beat dots + progress: app-level `createBeatDots` RAF loop, D9 pure-function beat phase, visibility pause/snap, reduced-motion static highlighting (B-01…B-05). Phase 7 E2E suite: 21 chromium tests with in-page timing loggers.
- **Aug 21** — Phase 8 docs, site link, final acceptance: user-confirmed 13-item manual checklist; 127 unit + 21 E2E green; world-review clean. **v1 committed** (`a0e3498`).
- The `building-web-apps` skill was grown during v1 and later moved to the repo root, shared with CollageMaker by symlink (`a6ecfc0`).

## v1 code review + remediation (Aug 22, 2026)

- Full code review: **1 CRITICAL** (C-1: per-keystroke clamp corrupts BPM/count-in typing — type `120`, get `250`/`2500`), **9 IMPORTANT** (file-load lock/race gaps, engine offset guard, U-09 deviation, dead parallel visual clock, inverted teardown ownership, DI asymmetry, unshared "4", 0-tests-can-pass runner), 33 nits.
- Light-BDD remediation plan (3 phases; RD-1…RD-6 fix contracts; R-* scenario IDs). All open decisions confirmed by user same day.
- **Phase 1 — Shippable Gate** (C-1, I-1…I-4, I-9 + nits N-16/N-20/N-22/N-23): commit-on-Enter/blur inputs with local draft text (steppers stay live); single-flight file load + lock check at one choke point; engine rejects `offset >= duration`; "Preview stopped" announcement; runner hardened on the Mocha Runner `end` event (the old `stats.tests` wait was vacuously true — see learnings). **142 unit + 23 E2E.** Commit `ecea38e`.
- **Phase 2 — Design Hygiene** (I-5…I-8, N-17/N-18/N-21): engine visual clock retired — `createBeatDots` is the *sole* visual-clock owner; teardown ownership flipped to `beforeUnmount` (listener removal → `stopAll` → `engine.dispose` → `fileLoader.release`); `createBeatDots` made fully DI (`raf`/`cancelRaf`/`matchMedia`/`isPageHidden`); `BEATS_PER_BAR` centralized in `beatGrid.js`; AGENTS.md + v1 plan docs aligned. **145 unit + 23 E2E**, world-review clean. Commit `5ed3822`.
- **Phase 3 — Nit Backlog** (21 items, scoped same day, executed in two batches): engine/fileLoader hygiene (N-1 scheduler interval cleared at the PLAYING flip — a 30-min song no longer runs ~72k no-op ticks; N-4 `startSequence` returns a frozen schedule snapshot; N-5 tooLong message interpolates the injected limit; N-6 `_finish(event)` extract; N-7 `ENGINE_EVENTS` constants) and app/template/E2E (N-8 targeted `errorMessage` clearing; N-9 `--beat-interval` set only on value change; N-10 static progressbar label + `:aria-valuetext`; N-12 “Count-in restarted” announcement; N-13 dragleave child-guard; N-14 post-resume-await unmount guard; N-15 dead scrubber `:tabindex` dropped; N-19 offset hint/placeholder `m:ss.t`; N-25/N-26/N-27 test-quality pins; docs KB-11/12/13 + U-18 scoping note). Deviation: batch-level commits (A: `4e915d5`, B/D: `d57bf21`) instead of per-item — the items share files. **158 unit + 23 E2E**, world-review clean (no issues). Deferred per OD-2: N-11 (behavior change), N-29…N-31 (mobile cluster), N-28 upgrade path.

## CR 001 — UI Enhancements: waveform progress + BPM detection (Aug 23–25, 2026)

- **Aug 23** — Change request authored, expanded, and world-reviewed (W-1…W-17 settled as constraints); 7-phase BDD implementation plan decomposed under `_agent_docs/plans/2026-08-23-cr-001-ui-enhancements/` (73 canonical scenario rows: WF-P/WF-V/WF-I, TD/TD-U, E2E-3.x/4.x; decisions D-A…D-M spot-verified against source). Plan-level corrections caught at authoring: the sine3s bucket arithmetic was inverted in the CR/memo (132 300 samples @ 4096 = 4096 buckets × ≈32 samples, NOT ≈32 buckets — pinned as WF-P1.4), and the memo's E2E-4.2 expectation contradicted CR §2.5's settled touched-flag lifecycle (flag resets on every load → re-drop re-suggests).
- **Aug 24** — **Phase 1 — Peaks + channel data (pure)** via TDD: new `MyESModules/Analysis/` — `channelData.js` (`channelArrays`/`mixDown`/`monoMixdown`; one shared mean per AGENTS.md “DO NOT Duplicate”) + `waveformPeaks.js` (`extractPeaks`/`poolPeaks`; 4096-bucket high-res pool, stride 4 above 20 M samples/channel, stereo min-of-mins/max-of-maxs, NaN/±Inf → 0, `Number.isFinite`-guarded, `sampleRate`-explicit) + `WaveformPeaksTest.html` (WF-P1.1…WF-P1.9; RED observed as the pinned R-11 barrel link-failure shape and console-verified before GREEN). Plan bug corrected in place: WF-P1.7's spike at “sample 40 000 000” was out of range for the 20 000 001-sample buffer → 4 000 000 (in-range stride-4 multiple). World-review: the one “critical” (main-thread cost for 30-min files) was refuted by a 59 ms benchmark of the actual worst case — the settled W-6 stride disposition stands, chunking remains the documented fallback; two JSDoc pins added (bucket b ↔ song-time fraction [b/B,(b+1)/B) alignment that Phase 4's playhead relies on; no-mutation aliasing contract for `channelArrays`/`mixDown`); the rest rejected as conflicts with pinned contracts (D-B guard scope, WF-P1.9 reference-identity, pinned 20 M-sample test size). **167 unit + 23 E2E.**
- **Aug 24** — **Phase 2 — Tempo detection (pure)** via TDD: `MyESModules/Analysis/tempoDetection.js` (`detectTempo` + `TEMPO`; mono mixdown → leading-silence trim → half-wave-rectified onset envelope → energy-normalized autocorrelation over the imported `BPM_MIN`/`BPM_MAX` range with a 70–180 prior, parabolic period refinement, out-of-band winners require a non-weak in-band alias else `null`, confidence = best/non-aliased-runner) + `TempoDetectionTest.html` (TD-1.1…TD-1.15 + pinned-constants test; RED observed as the pinned R-11 shape; zero real AudioContext — trains synthesized in-page from the `clickBuffers.js` recipe). One tuning-contract adjustment: `MIN_ONSET_FRAMES` 8 → 12 (the 75 %-overlap frame grid yields ≈2 onsets-above-floor per click — 6 clicks → 11, which the pinned 8 would have let through TD-1.11). World-review: **zero action items** — all five flagged deviations (tuning, two added `TEMPO` constants, refinement, plan-bug reading, W-8 formula) sanctioned; both discussion items were plan-doc errors corrected in place: (a) TD-1.3 and TD-1.7 pinned **byte-identical inputs** with contradictory outcomes (60 ± 1 vs 120-or-null-never-60) — a pure function cannot satisfy both; the P0 mechanism note governs, so TD-1.3 was corrected to `null`; (b) D-D's illustrative lag range `H·60/BPM` (≈123…1024 frames) was dimensionally missing `sampleRate` — corrected to `60·sampleRate/(BPM·HOP)` (≈21…172 @ 44.1 kHz). Measured worst case: **14 ms** (60 s @ 48 kHz) / 10.6 ms (30-min file, window clamps) — well under the 50 ms budget; the 14 ms is the citable constant for Phase 7. **183 unit + 23 E2E.**
- **Aug 24** — **Phase 3 — Waveform view factory** via TDD: `MyESModules/App/createWaveformView.js` (DI-pure `createWaveformView(vm, base, callbacks)` per the `createBeatDots` convention — pooled min/max columns one-per-CSS-column mirrored about center, RAF-coalesced with no-op dedupe, DPR backing store, optional pointer-scrub path: capture try/catch (W-1), pointercancel/blur/global-pointerup cleanup, commit→focus / discard→no-focus; idempotent `dispose` with GPU release) + `WaveformViewTest.html` (WF-V1.1…WF-V1.10 + 3 supporting rows; Proxy-mocked 2D ctx over a real offscreen context, fake RAF/canvas/window — zero global patching). RED first showed the pinned R-11 shape, then revealed a second, identical-signal cause: the new test page was missing its trailing `load → mocha.run()` listener — the runner's broken-page signal is a disjunction (new learning). Three as-built deltas vs D-F, all documented in context.md: `resize(widthCss)` one-arg height default (the W-5 storm scenarios are one-arg), `base.getWindow` (the listener map must be injectable for WF-V1.4's removal pins), and — caught by the phase-close handoff audit, not by any scenario test — **init performs the container-measured DPR sizing**: the pinned init contract says “DPR sizing” but the pinned Phase-4 wiring calls only `init` at mount (no window-resize event fires at mount), so without this the backing store would stay 0×0 and the waveform would never paint. Measured worst-case render: **0.006 ms** (poolPeaks 4096→640 + 640-column y-math, R-8 width bound). World-review: **zero action items** — all five briefed hypotheses verified; the three deltas sanctioned. **198 unit + 23 E2E.**
- **Aug 24** — **Phase 4 — Waveform wiring + playhead** via TDD: `onFileDropped`'s ok-branch gained the single post-load hook (D-E) — generation bump + `_bpmTouchedThisFile` reset + `waveformReady` placeholder reset, then `_schedulePostLoadTasks(buffer, generation)` in the **two-task shape from day one** (peaks live behind the pinned `setTimeout(0)` yield with the three-clause `_analysisValid` guard pre- and post-`extractPeaks`; tempo body a guarded no-op until Phase 6 fills it — the D-M constraint); `mounted()` builds `createWaveformView(this, {}, {})` display-only (no callbacks — the untouched `keyboard.spec` exact-Tab-order stays green, R-2) and `beforeUnmount` inserts `_disposed = true` (set FIRST so in-flight tasks die) + `_peaks`/`_buffer` nulling + `waveformView.dispose()` between `stopAll()` and `engine.dispose()`; the 8 px progress block was replaced with the D-H template (placeholder track → `role="img"` canvas → dim/offset/playhead DOM overlays; the progressbar role moved to `.progress-readout`, W-10; `.progress-fill` deleted, `progressPercent` retained per R-4); four new computeds (`displayPosition`/`formattedDisplayPosition`/`playheadPercent`/`markerPercent`) ride the existing 10 Hz `songPosition` throttle — zero new clocks (D9/KB-14). Unit: WF-I1.1…WF-I1.5 (the pinned yield driven by a FIFO `window.setTimeout` swap) + 4 computed rows; E2E: new `waveform.spec.cjs` E2E-3.1…3.4 (playhead tracking via the 5 ms in-page logger; height clamp 48/64 px). Two in-place plan-adjacent fixes: E2E-3.1 deviates from the plan's prescribed 5 ms logger by design — the ready→first-paint gap (one `setTimeout(0)` + ~1 ms extract ≈ 1.5–4 ms) is **sub-poll-period**, so a poll cannot order the two transitions (30–80 % flake); in-page MutationObservers record each transition at its exact timestamp instead (new learning); and the B-04 (rev) template pin in `BeatDotsTest.html` was updated in place for the settled W-10 role move (it pinned the pre-CR `.progress[role="progressbar"]` markup — the "fully green" criterion caught the cross-suite coupling). Phase-close handoff audit vs Phase 5 clean (canvas attr diff trivial; view exposes the three callbacks + `setDraft`). World-review: **zero action items** — all nine briefed hypotheses verified. **207 unit + 27 E2E.**
- **Aug 25** — **Phase 5 — Scrub + keyboard replaces the old offset range scrubber** via TDD: four new VM handlers in `createMetronomadMethods.js` — `onWaveformScrubStart`/`onWaveformScrubMove` (draft-only, `clampOffset`-quantized, `view.setDraft`) / `onWaveformScrubEnd` (commit → the shared `onOffsetScrub` law; cancel → discard, W-1) / `onWaveformKeydown` (←/→ ±0.1, Shift ±1, PageUp/Down ±10 % of duration, Home/End 0/duration; `preventDefault` only after a handled commit) — one quantization law end-to-end (T-35; the `onOffsetScrub` JSDoc re-pointed, R-5). The Phase-3 view's pointer path went live via three lifecycle-wired callbacks (call-time lookup arrows, I-7 shape); the canvas became the ARIA `slider` (`:tabindex="isReady ? 0 : -1"`, `:aria-disabled="isParamLocked || null"`); the old range input deleted (O-1) with every E2E reference re-pointed in the same change set (R-2). Unit: WF-I2.1…WF-I2.10 (mock-VM + `_waveformView` spy + fake keydown events; RED = `TypeError: not a function`). E2E: new E2E-3.5…3.10 in `waveform.spec.cjs` (click→commit+focus-return; drag tracking via the 5 ms in-page readout logger; synthetic `pointercancel` + `window` `blur` discard paths; keyboard parity; ARIA role split; canvas lock) — 217 unit + 33 E2E. Four as-built deltas, all world-review-sanctioned: (1) zero-duration backstops in scrubStart/keydown — D-G's verbatim showed only the `isParamLocked` guard, but the WF-I2.10 pin requires a total no-op at zero duration (`clampOffset(x, 0)` returns 0, so without the backstop a draft-of-0 write would still land); (2) `formatTime` exposed as a template method via the inline script (`methodsConfig: { …methods, formatTime }`) — the pinned `:aria-valuetext="'Offset ' + formatTime(offset)"` binding cannot reference a module-scope import from a Vue template; (3) **tab order**: the plan's prose said the canvas takes the scrubber's slot between `bpmPlusBtn` and `offsetInput`, but tab order is DOM order and the canvas lives in the progress block *below* the playback buttons — the canonical 8-element sequence ends with `waveformCanvas` (re-pinned in `keyboard.spec.cjs`; discussion item, not a layout change); (4) E2E-3.10 asserts `#app input[type="range"]` count 0 (the scrubber was the app's only range input) so the O-1 grep pass (`rg "offsetScrubber"` → zero hits in `index.html test/ MyComponents/ MyESModules/`) is literally satisfiable. Test-hygiene deltas: E2E-3.6 pins the *committed* readout before draining the in-page logger (the 5 ms sampler can coalesce ticks under load — first-sampled-tick asserted as a range, exact end pinned by the committed-readout wait); E2E-3.7's draft-live/discard assertions target the readout, not `#offsetInput` (the field mirrors the COMMITTED offset — pinned by WF-I2.3/2.4; the phase file's manual-checklist "field + readout mirror throughout" prose reads as the settled split: readout tracks the draft, field mirrors the commit). World-review: **zero action items** — all eight briefed hypotheses verified (backstops necessary per WF-I2.10; template-method exposure is the standard Vue pattern; no double-commit — `_endDrag` nulls `_dragPointerId` first; no float drift — `clampOffset` re-quantizes every step; `v-show` + `waveformReady` make the `decoding`-state tabindex/aria-disabled combination moot). Phase-close handoff audit vs Phase 6 clean (BPM group + `commitBpmEntry`/`onBpmStep`/`_restoreLastValid` anchors intact at the planned lines; the `_runTempoSuggestion` no-op and two-task hook untouched, D-M).
- **Aug 25** — **Phase 6 — Tempo suggestion integration** via TDD: `clicks20.wav` fixture first (D-K ground truth) — `scripts/make-clicks20-wav.cjs` (dependency-free; envelope math identical to `clickBuffers.js`) → 20 s · 44 100 Hz · mono · 16-bit PCM, 40 clicks @ 0.5 s (120 BPM, 1568 Hz accent every 4th), 1 764 044 bytes, byte-identical regeneration verified; sanity pass decodes the *encoded* bytes and checks all 40 onsets. Then D-I verbatim in `createMetronomadMethods.js`: `_runTempoSuggestion` body filled (the `_schedulePostLoadTasks` two-task hook untouched, D-M — grep-verified), `applyTempoSuggestion` four-clause gate read at write time (R-10) + the new `onBpmTextInput` (no-op except the `_bpmTouchedThisFile` flag; flag sites only in `commitBpmEntry`/`onBpmStep`, never `_restoreLastValid`, R-12); the pinned `Detected ~{{ tempoSuggestion }} BPM` hint `<p role="status">` + V-07 announcement string (the two owned strings, verbatim). Unit: TD-U1.1…TD-U1.10 + V-07 inventory row (RED = `TypeError: not a function` ×11); E2E: new `tempo.spec.cjs` E2E-4.1…4.4 (RED = no hint element ×3). Three as-built deltas vs the plan, all recorded: **(1) E2E-4.4 re-pinned in place** — the plan's "focused mid-draft survives the drop" is unreachable via ANY load trigger (a drop moves focus; even a focus-preserving load blurs `#bpmInput` because decoding flips `:disabled="!isReady"`, and the RD-1 commit-on-blur law then commits the draft — verified with in-page focusin/focusout traces); the row now pins the reachable literal-step behavior (blur-commit 9→30, flag reset, prefill lands visibly) and the W-16 no-clobber guarantee stays P0-unit (TD-U1.4/5). **(2) The prefill clears `bpmClamped`** — the D-H claim "the touched flag prevents hint coexistence" was factually wrong (a clamped commit + re-drop reproduced both hints simultaneously); the mutual exclusion is enforced by the in-range prefill write itself, pinned by TD-U1.1; context.md D-H corrected. **(3) The ok-branch clears `tempoSuggestion`** — the hint is per-file (the manual "drop a sine → no hint" criterion implies it; the plan never stated the clear). Test-hygiene deltas: TD-U1.8/1.9 use real 90/180 BPM click-train buffers (a constant buffer yields `detectTempo` null and makes the guard test vacuously true for the wrong reason); E2E-4.3/4.4 absence assertions use an in-page 500 ms MutationObserver + sweep watcher (node-side absence polls pass from t=0 and can't prove silence during resolution); the negative rows assert "still the load announcement", not empty live region. World-review: one Warning (UX-6 — the prefill is the first event announced *twice*: hint `role="status"` + live region; both halves CR-pinned, so it is a **discussion item** routed to the Phase-7 VoiceOver checklist — the remedy would be a CR-level spec change, not an in-plan edit) and one Suggestion applied (JSDoc: `v-model` is a directive, not an `@input` listener); the fixture decode-cost question closed as measured-in-budget (PERF-1). **228 unit + 37 E2E.**
- **Aug 25** — **Phase 7 — Docs + acceptance** (docs phase, no TDD): full suites re-verified green (228/228 unit, 37/37 E2E); plan integrity check OK (63 distinct scenario IDs); all grep gates pass (`offsetScrubber` zero hits incl. AGENTS.md; KB-14…KB-17 present after KB-13). `AGENTS.md` brought to as-built: Architecture gains the `Analysis/` pure-signal modules (one shared mean; `detectTempo`'s pure DSP pipeline), `createWaveformView` (DI-pure; owns only its canvas + listeners; canvas is the offset **slider** that replaced the old offset range input — zero `input[type="range"]` in the app), the playhead riding the existing 10 Hz `songPosition` throttle (KB-14), and the BPM-suggestion contract (write-time gate, W-16; null → silence, W-13; prefill clears `bpmClamped`); Directory/Testing/Conventions updated (new test suites, `clicks20.wav` provenance + regen script, no-real-AudioContext convention for tempo/view suites, the two owned strings, `_analysisValid` generation-guard discipline, extended `beforeUnmount` order with `_disposed`-first + `waveformView.dispose()`); v1 `context.md` gained KB-14…KB-17 verbatim. One plan-internal conflict resolved: the phase's Directory prose (name the replaced `#offsetScrubber`) contradicts the phase's own success criterion (`rg "offsetScrubber"` → zero hits, AGENTS.md included) — the gate won; the docs name the replacement token-free. Final whole-change-set world-review: **zero action items** — every doc claim verified line-by-line against source; cross-cutting risks (peaks GC across re-loads, canvas backing-store release, the two post-Ready tasks racing, `offsetDraft` lifecycle vs appState, reduced-motion, narrow viewport, iOS `pointercancel`) all verified safe; UX-6 (tempo prefill announced twice — hint `role="status"` + live region) remains the single discussion item, routed to the manual VoiceOver pass. **CR 001 automation complete** — the manual acceptance checklist (dual-SR pass incl. UX-6, real-song tempo accuracy, playback regression by ear, low-end mobile, memory cycles) awaits user sign-off. Phases 1–7 uncommitted (commit is `build-quick-work`'s responsibility).

## Test-count trajectory

| Milestone | Unit | E2E |
|-----------|------|-----|
| v1 shipped (Aug 21) | 127 | 21 |
| Review Phase 1 (Aug 22) | 142 | 23 |
| Review Phase 2 (Aug 22) | 145 | 23 |
| Review Phase 3 (Aug 22) | 158 | 23 |
| CR 001 Phase 1 (Aug 24) | 167 | 23 |
| CR 001 Phase 2 (Aug 24) | 183 | 23 |
| CR 001 Phase 3 (Aug 24) | 198 | 23 |
| CR 001 Phase 4 (Aug 24) | 207 | 27 |
| CR 001 Phase 5 (Aug 25) | 217 | 33 |
| CR 001 Phase 6 (Aug 25) | 228 | 37 |
| CR 001 shipped (Aug 25) | 228 | 37 |

**Current state (2026-08-25):** 228/228 unit, 37/37 E2E (chromium). Server must be user-started on :8000 for both suites. **CR 001 complete (automation + docs)** — Phases 1–7 done: pure Analysis modules (peaks + tempo), waveform view factory, waveform visible with playhead + post-load hook, canvas scrub + keyboard (the old offset range scrubber is gone), tempo prefill live with the `clicks20.wav` round-trip, AGENTS.md + v1 KB-14…KB-17 aligned to as-built, final world-review clean. Remaining: user sign-off of the manual acceptance checklist (phase-7-docs-acceptance.md §3), then the commit (build-quick-work).
