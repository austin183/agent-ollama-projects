# Background Colors and Font Color Options
- Right now, the color options show the hex value for the chosen color.
- Could we change that so instead of a color name, it just shows a larger color button of the selected color?