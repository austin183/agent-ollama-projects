# Big Idea

We have the project in `~/workspace/agent-ollama-projects/Experiments/CollageMaker` for a macOS app that accepts a set of pictures, offers ways to edit them together into a Collage, and export them as a JPEG file. We want to rewrite this as a web application that could run from a static website, part of `~/workspace/austin183.github.io` as a new site there, in a similar way as `~/workspace/austin183.github.io/MidiSongBuilder/Midiestro3D.html` is hosted there.

**Current location:** `~/workspace/austin183.github.io/CollageMaker/`
