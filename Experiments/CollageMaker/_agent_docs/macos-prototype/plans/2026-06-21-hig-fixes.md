# HIG Review Fixes — Implementation Plan

**Date:** 2026-06-21
**Source:** `reviews/2026-06-21-hig-review-meg.md`, `reviews/2026-06-21-hig-review-kate.md`

---

## In Scope

### P1: Accessibility

#### 1. Wire `reduceMotion` in `ExportPanel`
- **File:** `Views/ExportPanel.swift`
- Add `@Environment(\.accessibilityReduceMotion) private var reduceMotion`
- Line 249: `.transition(.opacity)` → `.transition(reduceMotion ? .identity : .opacity)`

#### 2. Accessibility labels in `SettingsView`
- **File:** `Views/SettingsView.swift`
- Add `.accessibilityLabel`/`.accessibilityValue` to:
  - "Default Layout" picker (line 99)
  - "Default Gutter" slider (line 113)
  - "Background Style" picker (line 125)
  - Solid/gradient color wells (lines 135, 142, 149)
  - "Gradient angle" slider (line 161)
  - "Title" TextEditor (line 180)
  - Font picker (line 204)
  - "Font Size" slider (line 193)
  - "Choose Folder" button (line 221)
  - "Quality" slider (line 248)

### P2: Windowing & Canvas

#### 3. Minimum window size
- **File:** `ContentView.swift`
- Add `.frame(minWidth: 750, minHeight: 500)` on the `NavigationSplitView` (after line 62, before `.alert`)

#### 4. Descriptive panel accessibility labels
- **File:** `Views/CollageEditorView.swift`
- Line 306: Change `.accessibilityLabel("Image panel")` to include panel index + filename, e.g. `"Panel 1, photo.jpg"`
- Need to pass index/filename info into `PanelOverlay` from `PanelsOverlayView` (which iterates `ForEach(viewModel.panels)`)

### P2: Export Cancellation

#### 5. Cancel button during export
- **File:** `ViewModel/CollageViewModel.swift` — add `cancelExport()` that calls `exportManager.exportTask?.cancel()`
- **File:** `Views/ExportPanel.swift` lines 219-238 — when `viewModel.isExporting`, swap button to call `cancelExport()` with label "Cancel"

### P3: Drag Feedback

#### 6. Dashed stroke for drag target
- **File:** `Views/DropPreviewView.swift`
- Line 29: Change target panel (green) from solid to dashed stroke to distinguish from source panel (cyan, solid)
- Will need a custom `Shape` since `.lineStyle` is a Shape-only modifier

---

## Out of Scope (by user decision)

- Delete key handler for selected panels
- `backgroundExtensionEffect()` on sidebar (Liquid Glass effect is distracting)
- Cmd+S → Cmd+E change
- Modal alert for export errors
