# Composite rendering for main image
As opposed to rendering the whole image every time a user pans or zooms in a panel, we should only render the changes to that one panel.  Then Export would be the only action that assembles the whole picture.
