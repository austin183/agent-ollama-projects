# Panel Editor
- Remove the Image selector
  - It makes it possible to set the same image in two panels
  - Drag and drop panel reordering supersedes the need for this
- Maybe we could replace it with a preview of the full size selected panel image with a rectangle outline showing what part of the image is visible in the paenl

# Images array
- Changing the starred image should recompute the main image layout so the starred image becomes the Hero image for layouts with Hero images
  - Changing the starred image currently does nothing.

# Layout
- What does the image array under Layouts do?

# Status
- What does the Ready light mean?

# Title Sytle
- The Font drop down is two menus instead of one
- Can the drop down be WYSIWYG?
- The icons for "left, right, center" and the checkbox for a background seem weird
  - Could we use the paragraph icons for the "left, right, center"
  - Is there an icon for the background checkbox that could make it clear what that does?