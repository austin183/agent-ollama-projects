# Test Run Reduction
The quiet parameter in the run_tests.sh file still prints out alot of information about the test run that should only be interesting if the tests failed.  We need to see how else we could run the test command that does not print out so much information.

