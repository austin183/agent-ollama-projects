# MariaDB root password (used by both primary and replica)
# Change this to a strong password before deploying
MARIADB_ROOT_PASSWORD=CHANGE_ME

# Password for the 'replicator' application user
# This user is used by applications and for replication setup
REPLICATOR_PASSWORD=CHANGE_ME
