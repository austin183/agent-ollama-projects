# MariaDB root password - used for database administration
# Change this to a strong password before production use
MARIADB_ROOT_PASSWORD=changeme_root

# MariaDB application user password - used by homelab user
# Change this to a strong password before production use
MARIADB_PASSWORD=changeme123
