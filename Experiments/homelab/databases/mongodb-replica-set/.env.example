# MongoDB root password for replica set authentication
# This password is used for the admin user across all replica set members
# Generate a strong password: openssl rand -base64 32
MONGO_ROOT_PASSWORD=CHANGE_ME
