version: '3.8'

services:
  container-a:
    image: alpine:latest
    command: sleep 3600
    networks:
      - test-network

  container-b:
    image: alpine:latest
    command: sleep 3600
    networks:
      - test-network

networks:
  test-network:
    driver: bridge
